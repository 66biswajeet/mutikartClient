# Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         MULTIKART E-COMMERCE                    │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────┐         ┌─────────────────────────────┐
│   ADMIN PANEL (3000)    │         │   CLIENT SITE (3001)        │
│  ────────────────────   │         │  ─────────────────────────  │
│                         │         │                             │
│  ┌──────────────────┐  │         │  ┌──────────────────────┐   │
│  │  Product CRUD    │  │         │  │   Homepage           │   │
│  │  ┌────────────┐  │  │         │  │   ┌──────────────┐   │   │
│  │  │ Add        │  │  │         │  │   │ Hero Section │   │   │
│  │  │ Edit       │  │  │         │  │   │ Categories   │   │   │
│  │  │ Delete     │  │  │         │  │   │ ProductFeed  │◄──┼───┼─┐
│  │  │ List       │  │  │         │  │   └──────────────┘   │   │ │
│  │  └────────────┘  │  │         │  └──────────────────────┘   │ │
│  └──────────────────┘  │         │                             │ │
│           │            │         │  ┌──────────────────────┐   │ │
│           ▼            │         │  │ API Routes           │   │ │
│  ┌──────────────────┐  │         │  │  /api/products       │───┼─┘
│  │  MongoDB         │  │         │  │  ┌────────────────┐  │   │
│  │  ┌────────────┐  │  │         │  │  │ Transform     │  │   │
│  │  │ Products   │  │  │         │  │  │ Cache (60s)   │  │   │
│  │  │ Categories │  │  │         │  │  │ Error Handle  │  │   │
│  │  │ Brands     │  │  │         │  │  └────────────────┘  │   │
│  │  └────────────┘  │  │         │  └──────────────────────┘   │
│  └──────────────────┘  │         │           │                 │
│           │            │         │           │                 │
│           ▼            │         │           ▼                 │
│  ┌──────────────────┐  │         │  ┌──────────────────────┐   │
│  │  API Endpoint    │  │◄────────┼──┤  Fetch Request       │   │
│  │  /api/product    │  │  HTTP   │  │  fetch(admin_url)    │   │
│  └──────────────────┘  │         │  └──────────────────────┘   │
│                         │         │                             │
└─────────────────────────┘         └─────────────────────────────┘
```

## Data Flow

```
1. Admin adds product
   ↓
2. Saved to MongoDB
   ↓
3. Product appears in admin list
   ↓
4. Client page loads
   ↓
5. ProductFeed component renders
   ↓
6. Fetches from /api/products
   ↓
7. Client API proxies to admin
   ↓
8. Admin returns product data
   ↓
9. Client transforms data
   ↓
10. ProductCard displays product
    ↓
11. User sees product on site! 🎉
```

## Component Hierarchy

```
HomePage (page.js)
│
├── Header
│   ├── Logo
│   ├── SearchBar
│   └── UserMenu
│
├── CategoryBar
│   └── CategoryLinks[]
│
├── Sidebar (conditionally rendered)
│   └── CategoryTree[]
│
├── Main Content
│   ├── HeroSection
│   │   ├── Title
│   │   ├── Description
│   │   └── CTAButtons
│   │
│   ├── FeaturedCategories
│   │   └── CategoryCard[]
│   │
│   ├── ProductFeed ⭐
│   │   ├── Header
│   │   ├── ProductGrid
│   │   │   └── ProductCard[]
│   │   │       ├── Image
│   │   │       ├── Title
│   │   │       ├── Description
│   │   │       ├── Price
│   │   │       ├── Badge (conditional)
│   │   │       └── ActionButtons
│   │   └── LoadMoreButton
│   │
│   └── AuthPrompt
│       ├── Icon
│       ├── Content
│       ├── CTAButtons
│       └── Features
│
└── Footer
    ├── BrandSection
    ├── LinkColumns[]
    └── BottomSection
```

## API Request Flow

```
┌────────────┐
│  Browser   │
└──────┬─────┘
       │ 1. GET /
       ▼
┌─────────────────┐
│  Next.js Page   │
│  (page.js)      │
└──────┬──────────┘
       │ 2. Render ProductFeed
       ▼
┌──────────────────────┐
│  ProductFeed         │
│  useEffect(() => {   │
│    fetch('/api/...')│
│  })                  │
└──────┬───────────────┘
       │ 3. fetch('/api/products?page=1&limit=12')
       ▼
┌────────────────────────────────┐
│  Client API Route              │
│  /api/products/route.js        │
│  ┌──────────────────────────┐  │
│  │ 1. Parse query params    │  │
│  │ 2. Build admin URL       │  │
│  │ 3. Fetch from admin      │  │
│  │ 4. Transform response    │  │
│  │ 5. Add badges            │  │
│  │ 6. Return JSON           │  │
│  └──────────────────────────┘  │
└──────┬─────────────────────────┘
       │ 4. fetch(ADMIN_API_URL/api/product)
       ▼
┌──────────────────────────┐
│  Admin API               │
│  /api/product/route.js   │
│  ┌────────────────────┐  │
│  │ 1. Auth check      │  │
│  │ 2. Query MongoDB   │  │
│  │ 3. Populate refs   │  │
│  │ 4. Return products │  │
│  └────────────────────┘  │
└──────┬───────────────────┘
       │ 5. Query MongoDB
       ▼
┌──────────────────┐
│    MongoDB       │
│  ┌────────────┐  │
│  │  Products  │  │
│  │  ├─ name   │  │
│  │  ├─ media  │  │
│  │  ├─ price  │  │
│  │  └─ ...    │  │
│  └────────────┘  │
└──────┬───────────┘
       │ 6. Return documents
       ▼
(Flow continues back up)
       │
       ▼
┌────────────────────┐
│  Browser           │
│  ┌──────────────┐  │
│  │ ProductCard  │  │
│  │ displays!    │  │
│  └──────────────┘  │
└────────────────────┘
```

## State Management

```
ProductFeed Component State:
┌────────────────────────────┐
│ products: []               │ ← Product array
│ loading: true/false        │ ← Loading indicator
│ error: null/string         │ ← Error message
│ pagination: {              │ ← Pagination info
│   currentPage: 1,          │
│   lastPage: 5,             │
│   total: 50,               │
│   perPage: 12              │
│ }                          │
└────────────────────────────┘

State Transitions:
┌─────────┐  fetchProducts()  ┌──────────┐
│ Initial │ ───────────────> │ Loading  │
│ loading │                  │ true     │
└─────────┘                  └────┬─────┘
                                  │
                     ┌────────────┼────────────┐
                     │                         │
                  Success                   Failure
                     │                         │
                     ▼                         ▼
              ┌─────────────┐          ┌────────────┐
              │ Loaded      │          │ Error      │
              │ products: […]│          │ error: msg │
              │ loading: false│         │ loading: false│
              └─────────────┘          └────────────┘
```

## File Structure

```
client/
├── src/
│   ├── app/
│   │   ├── api/
│   │   │   └── products/
│   │   │       └── route.js       ← Proxy to admin API
│   │   ├── page.js                ← Homepage
│   │   └── layout.js              ← Root layout
│   │
│   ├── components/
│   │   ├── ProductFeed/
│   │   │   ├── ProductFeed.js     ← Fetches & displays products
│   │   │   └── ProductFeed.module.css
│   │   ├── ProductCard/
│   │   │   ├── ProductCard.js     ← Individual product display
│   │   │   └── ProductCard.module.css
│   │   ├── Header/
│   │   ├── Footer/
│   │   └── ...
│   │
│   ├── hooks/
│   │   ├── useProducts.js         ← Product fetching hook
│   │   └── useScroll.js
│   │
│   └── utils/
│       └── api.js                 ← API helper functions
│
├── .env.local                     ← Environment config
├── next.config.js                 ← Next.js config
└── package.json
```

## Technology Stack

```
┌──────────────────────────────────────────┐
│           PRESENTATION LAYER             │
│  ┌────────────────────────────────────┐  │
│  │     React Components (JSX)         │  │
│  │     CSS Modules (Scoped Styles)    │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────┐
│         APPLICATION LAYER                │
│  ┌────────────────────────────────────┐  │
│  │    Next.js 14 (App Router)         │  │
│  │    React Hooks (State Management)  │  │
│  │    Custom Hooks (useProducts)      │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────┐
│           API LAYER                      │
│  ┌────────────────────────────────────┐  │
│  │    Next.js API Routes              │  │
│  │    Fetch API (HTTP Client)         │  │
│  │    Data Transformation Layer       │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
                    │
┌──────────────────────────────────────────┐
│           DATA LAYER                     │
│  ┌────────────────────────────────────┐  │
│  │    Admin Backend API               │  │
│  │    MongoDB (via Admin)             │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

## Deployment Architecture

```
┌───────────────────────────────────────────┐
│             PRODUCTION                    │
└───────────────────────────────────────────┘

┌──────────────┐         ┌──────────────────┐
│   Vercel     │         │   Heroku/VPS     │
│  (Client)    │ ──────> │  (Admin API)     │
│  Port 443    │  HTTPS  │  Port 443        │
└──────────────┘         └──────────────────┘
       │                           │
       │                           │
       ▼                           ▼
┌──────────────┐         ┌──────────────────┐
│   CDN        │         │   MongoDB Atlas  │
│  (Static)    │         │   (Database)     │
└──────────────┘         └──────────────────┘

Environment Variables:
- Client: NEXT_PUBLIC_ADMIN_API_URL=https://admin.example.com
- Admin: MONGODB_URI=mongodb+srv://...
```

---

**Visual Guide Version:** 1.0  
**Last Updated:** December 14, 2025
